<%= @header.key.ljust(15) %> - <%= @header.summary %><%= @header.fix_versions.empty? ? '' : "  - (#{@header.fix_versions.collect{|p| render_versions(p)}.join(',')})" %>  - <%= render_priority(@header.priority) %> - <%= @header.company %> - <%= render_status(@header.status) %> - <%= render_resolution(@header.resolution) %>
